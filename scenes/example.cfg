# Antialiasing test configuration
camera:
{
    resolution = { width = 800; height = 600; };
    position   = { x = 0; y = -100; z = 20; };
    rotation   = { x = 0; y = 0; z = 0; };
    fieldOfView = 72.0;
    antialiasing = 4;  # 2x2 = 4 samples per pixel (2, 3, 4, etc.)
};

primitives:
{
    spheres = (
        {
            x = 60;
            y = 5;
            z = 40;
            r = 25;
            material = {
                type = "Color";
                color = { r = 255; g = 64; b = 64; };
            };
        },
        {
            x = -40;
            y = 20;
            z = -10;
            r = 35;
            material = {
                type = "Color";
                color = { r = 64; g = 255; b = 64; };
            };
        }
    );

    planes = (
        {
            axis = "Z";
            position = -20;
            material = {
                type = "Color"
                color = { r = 64; g = 64; b = 255; };
            };
        }
    );
};

lights:
{
    ambient = 0.4;
    diffuse = 0.6;

    point = (
        {
            x = 400;
            y = 100;
            z = 500;
        }
    );

    directional = ();
};
